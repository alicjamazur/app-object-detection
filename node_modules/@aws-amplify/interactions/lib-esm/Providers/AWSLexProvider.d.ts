import { AbstractInteractionsProvider } from '@aws-amplify/interactions/lib-esm/Providers/InteractionsProvider';
import { InteractionsOptions, InteractionsMessage } from '@aws-amplify/interactions/lib-esm/types';
export declare class AWSLexProvider extends AbstractInteractionsProvider {
    private lexRuntimeServiceClient;
    private _botsCompleteCallback;
    constructor(options?: InteractionsOptions);
    getProviderName(): string;
    reportBotStatus(data: any, botname: any): void;
    sendMessage(botname: string, message: string | InteractionsMessage): Promise<object>;
    onComplete(botname: string, callback: any): void;
}
